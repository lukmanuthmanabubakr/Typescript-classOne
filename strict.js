function miniCalc(a, b, sign) {
  let result;
  if (sign == "+") {
    result = a + b;
  }
}
miniCalc(3, 6, "+");

function checkAge(age) {
  let result = age > 18 ? "Adult" : "Minor";
  console.log(result);
}
checkAge(5)
